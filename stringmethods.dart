void  main() {
  // String name = "Saim Raza";
  // String name1 = " Zaidi";
  // print(name.length);
  // print(name.isEmpty);
  // print(name.isNotEmpty);
  // print(name.startsWith("Sa"));
  // print(name[0]);
  // print(name.contains("i"));
  // print(name.split(""));
  // print(name.toLowerCase());
  // print(name.toUpperCase());
  // print(name.trim());
  // print(name.compareTo(name1)); // -1 cause length of name is less
  // String fullName = name + name1;
  // print(fullName.replaceAll("Zaidi", "Raza Zaidi")); // gap also matters
  // print(name.replaceAll("a", "d"));
  // print(name.substring(4));
  // print(name.substring(5, name.length));
  // print(name.substring(0, 4));
  // print(name.indexOf("i"));
  // print(name.endsWith("s"));
  // print(name.lastIndexOf("a"));
  // print(name.replaceRange(5, 9, "Zaidi"));
  // String text = "Hello Saim, Saim";
  // print(text.replaceFirst("Saim", "Dart"));
}