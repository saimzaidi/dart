void main() {
  // List list = [1, 2, 1, 2, 3, 5, 1, 3, 9];
  // // print(list.indexOf(1));
  // // print(list.lastIndexOf(1));
  // print(list.indexWhere((e) => e % 2 == 0)); // 1(2) 
  // print(list.lastIndexWhere((e) => e % 2 == 0)); // 3
}